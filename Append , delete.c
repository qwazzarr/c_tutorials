#include <stdio.h>
#define N 20

int main(int argc, char const *argv[])

{
    int A[N] = {0};
    int top = 0;
    for (int i = 0,i==20,++i)
        A[top] = i;
        top+=1;
    printf("%d",A);
    return 0;
}
