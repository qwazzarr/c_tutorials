#include <stdio.h>
#define N 20

int main(int argc, char const *argv[])

{
    int A[N] = {0};
    int top = 0;
    for (int i = 0;i<20;++i){
        A[i] = i; vx
        top+=1;
        printf("%d %d\n",top,i);
    }
    for (int b = 0;b<20;++b)
        printf(" %d", A[b]);
    return 0;
}
